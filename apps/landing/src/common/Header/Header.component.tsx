import * as Styled from './Header.styled';
import { Heading, Navigation } from '@/common';

const Header = () => (
  <Styled.Header>
    <Heading />
    <Navigation />
  </Styled.Header>
);

export default Header;
